app:
  url: http://localhost:8080
  security:
    jwt-secret: "jwt-tokens-that-should-be-changed-production"
    jwt-expiration-minutes: 30
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/educeasy_db?useSSL=false&serverTimezone=UTC
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
      hibernate.format_sql: true
      hibernate.show_sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration/{vendor}
    validate-on-migrate: true
    clean-disabled: true
    baseline-on-migrate: true
    baseline-version: 0.0.0.0
  mail:
    host: smtp.gmail.com
    port: 587
    username: aide.educeasy@gmail.com
    password: tmro dnfd bwfn zpcw
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
    default-encoding: UTF-8
server:
  port: 4700
  error:
    include-message: always
    include-binding-errors: always
logging.level.org.springframework.security: INFO
